# ELDYN v6.5 날짜 보호 패치

- 앱의 오늘 날짜를 `Asia/Seoul` 기준으로 통일했습니다.
- 미래 날짜를 단순히 열어본 것만으로 빈 `daily_logs` 행이 Supabase에 생성되지 않도록 했습니다.
- 이미 생성된 미래 날짜의 빈 기본 행은 로그인/동기화 시 자동 정리합니다.
- 음식, 러닝, 완료 운동, 메모 등 실제 기록이 있는 미래 데이터는 삭제하지 않습니다.
- 클라우드 복원 후 화면 날짜를 한국 기준 오늘로 되돌립니다.

## 배포 후 확인
1. 배포 후 Safari URL로 접속합니다.
2. Supabase `daily_logs`에서 빈 `2026-08-01` 행이 제거되는지 확인합니다.
3. 7월 31일 식단을 저장하고 새로고침한 뒤 그대로 보이는지 확인합니다.
