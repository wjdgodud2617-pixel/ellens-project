# ELDYN v1.2.22.10 — Run/Walk Story UI + 1KM Display

- 러닝/걷기 인증샷의 운동 타입을 `RUNNING` / `WALKING`으로 통일했습니다.
- 운동 인증샷과 같은 제목 위계로 표시되도록 조정했습니다.
- Distance / Pace / Time 숫자 크기를 키워도 항목 제목과 숫자가 겹치지 않도록 라벨-값 간격을 글자 크기에 따라 동적으로 계산합니다.
- GPS 거리가 반올림 기준 1000m에 도달하면 `1000 M`을 거치지 않고 즉시 KM 표기로 전환합니다.
- GPS 거리 계산, Split 계산, 평균속도/페이스, 식단, Calendar 로직은 변경하지 않았습니다.
