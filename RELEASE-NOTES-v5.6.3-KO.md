# ELDYN v5.6.3 데이터 복구 패치

- Supabase `daily_logs.payload`의 식단(`mealPlan`)을 다시 화면 상태로 복구
- 각 날짜 로그 안의 러닝 기록(`runs`)을 전역 러닝 히스토리로 재구성
- 러닝 기록 중복 제거
- 식단 항목의 `foodItems` 안전 복원 및 영양 합계 재계산
- 서비스워커 캐시 버전 갱신

배포 후 기존 계정으로 로그인하고 앱을 새로고침하면 자동 복구됩니다.
