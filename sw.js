# ELDYN 동기화 마무리

Supabase 주소와 공개 키는 이미 `config.js`에 연결되어 있습니다.

## 남은 작업 1: 데이터베이스 만들기

1. Supabase 프로젝트에 로그인합니다.
2. 왼쪽 메뉴에서 **SQL Editor**를 엽니다.
3. **New query**를 누릅니다.
4. 이 폴더의 `supabase.sql` 내용을 전부 복사해 붙여넣습니다.
5. 오른쪽 아래 **Run**을 누릅니다.

## 남은 작업 2: 이메일 로그인 설정 확인

1. 왼쪽 메뉴에서 **Authentication → Providers → Email**을 엽니다.
2. Email 로그인이 활성화되어 있는지 확인합니다.
3. 가입 직후 바로 로그인하고 싶다면 이메일 확인 옵션을 끌 수 있습니다.
   개인용 앱이라면 편하지만, 외부 배포 시에는 이메일 확인을 켜는 편이 안전합니다.

## 실행

압축을 푼 폴더에서 터미널을 열고 실행합니다.

```bash
python -m http.server 8080
```

브라우저에서 아래 주소로 접속합니다.

```text
http://localhost:8080
```

앱의 Settings 화면에서 이메일과 비밀번호로 가입한 뒤 로그인하면 동기화가 활성화됩니다.

## 모바일과 PC에서 함께 사용하기

같은 주소를 두 기기에서 열어야 하므로, 폴더를 Netlify·Vercel·GitHub Pages 같은 HTTPS 호스팅에 배포해야 합니다. 로컬 주소인 `localhost`는 현재 기기에서만 열립니다.
